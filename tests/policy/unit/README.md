# Policy unit tests — Setup Topic 16
#
# Offline Kyverno CLI tests (no cluster). Does not replace live kubectl admission checks in Topic 07.
#
# Local:
#   kyverno test tests/policy/unit
#
# CI:
#   policy_test job in .gitlab-ci.yml

See fixtures under this directory and `kyverno-test.yaml`.
